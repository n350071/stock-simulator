<%= form_with(model: ticker_week_history, local: true) do |form| %>
  <% if ticker_week_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticker_week_history.errors.count, "error") %> prohibited this ticker_week_history from being saved:</h2>

      <ul>
        <% ticker_week_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :open %>
    <%= form.number_field :open %>
  </div>

  <div class="field">
    <%= form.label :high %>
    <%= form.number_field :high %>
  </div>

  <div class="field">
    <%= form.label :low %>
    <%= form.number_field :low %>
  </div>

  <div class="field">
    <%= form.label :close %>
    <%= form.number_field :close %>
  </div>

  <div class="field">
    <%= form.label :volue %>
    <%= form.number_field :volue %>
  </div>

  <div class="field">
    <%= form.label :week_at %>
    <%= form.date_select :week_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
